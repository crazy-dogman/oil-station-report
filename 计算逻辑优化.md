# Gas Station 计算逻辑优化方案

## 📋 概述

本文档基于原始计算逻辑.txt进行系统性优化，提供完整的加油站销售数据计算规范和标准报告格式。

## 🎯 标准报告格式

### 报告模板结构
```
{加油站名称}{YYYY-MM-DD}:

一、92#汽油销售{X}吨，95#汽油{X}吨，98#汽油销售{X}吨，共计汽油销售{X}吨，柴油{X}吨，柴汽合计销售{X}吨。本月累计销售汽油{X}吨，柴油{X}吨，共销售油品{X}吨。

二、环比昨日{增加/减少}{X}吨，{增幅/减幅}{X}％，其中汽油{增加/减少}{X}吨，{增幅/减幅}{X}%，柴油{增加/减少}{X}吨，{增幅/减幅}{X}%

三、当日非油销售{X}元，环比昨日{增加/减少}{X}元，{增幅/减幅}{X}%，{同比去年段，可选}本月累计{X}元。

四、当日电子券核销{X}张，累计核销{X}张。

五、工行积分兑换{X}笔，兑换金额{X}元

六、当日工行ETC{X}单。

七、三端订单消费{X}笔，实际评价数量{X}笔，测评率{X}％

八、今日办理电子卡{X}张
```

- 报告头部直接拼接“站名 + YYYY-MM-DD + 冒号”，符合 `generateReportHeader()` 现状。
- 第三段的“同比去年”文案仅在 `nonOilSales.lastYear > 0` 时插入，否则被完整省略。

## 🧮 核心计算逻辑

### 1. 升转吨计算（与当前实现保持一致）

#### 汽油密度转换
- **公式**: `吨数 = 升数 × 0.00075`
- **精度**: 直接 `Number((升数 × 0.00075).toFixed(2))`，无额外 4 位小数缓存
- **适用**: 92#/95#/98#
- **备注**: 计算过程会写入 `calculationProcesses[type]` 供 UI 展示

```javascript
function calculateGasolineTons(liters) {
    const value = parseFloat(liters) || 0;
    if (value <= 0) return 0;
    const result = value * 0.00075;
    return Number(result.toFixed(2)); // index.html 中的真实实现
}
```

#### 柴油密度转换
- **公式**: `吨数 = 升数 × 0.00085`
- **精度**: 同样直接 `toFixed(2)` 后转数字

```javascript
function calculateDieselTons(liters) {
    const value = parseFloat(liters) || 0;
    if (value <= 0) return 0;
    const result = value * 0.00085;
    return Number(result.toFixed(2));
}
```

### 2. 汇总与累计

`calculateTotals()` 会在任一油品吨数更新后触发，核心步骤如下：

```javascript
function calculateTotals(state) {
    const r = state.calculatedResults;
    r.gasolineTotal = Number((state.formData.gasoline92.tons +
                              state.formData.gasoline95.tons +
                              state.formData.gasoline98.tons).toFixed(2));

    r.totalSales = Number((r.gasolineTotal + state.formData.diesel.tons).toFixed(2));

    state.calculateMonthlyCumulative();      // 昨日累计 + 今日销量
    state.calculateMonthlyNonOilTotal();     // 非油昨日累计 + 今日金额
    state.calculateCumulativeVouchers();     // 电子券昨日累计 + 今日核销

    if (state.yesterdayData.parsed) {
        state.calculateComparison();
    }
    state.calculateEvaluationRate();
}
```

- **月累计**：若存在昨日数据，采用“昨日月累计 + 今日销量”；否则直接等于今日销量。
- **本月非油累计**：`monthlyNonOil = (yesterdayMonthlyNonOil || 0) + todayNonOil`，保留 2 位小数。
- **累计电子券核销**：`cumulativeVouchers = (yesterdayVouchersTotal || 0) + todayCount`。

### 3. 环比计算（与现有实现保持同步）

#### 环比变化量
- **公式**: `变化量 = 今日数据 - 昨日数据`
- **正值**: 增加
- **负值**: 减少

```javascript
function calculateChange(today, yesterday) {
    return today - yesterday;
}
```

#### 环比变化率
- **公式**: `变化率 = (今日数据 - 昨日数据) / 昨日数据 × 100%`
- **精度**: `Number(((change / yesterday) * 100).toFixed(2))`
- **特殊处理**: 昨日数据缺失或为 0 时直接判定“无法计算”，UI 展示提示文案

```javascript
function calculateChangeRate(today, yesterday) {
    if (!yesterday) {
        return {
            change: 0,
            changeRate: 0,
            changeType: '无法计算',
            changeRateType: '无法计算',
            processText: '缺少昨日数据，无法计算环比'
        };
    }
    const change = Number((today - yesterday).toFixed(2));
    const rate = Number(((change / yesterday) * 100).toFixed(2));
    return {
        change: Math.abs(change),
        changeRate: Math.abs(rate),
        changeType: change >= 0 ? '增加' : '减少',
        changeRateType: rate >= 0 ? '增幅' : '减幅'
    };
}
```

#### 环比描述自动生成
```javascript
function generateChangeDescription(result) {
    if (!result || result.changeType === '无法计算') {
        return '缺少昨日数据，无法计算环比';
    }
    return `${result.changeType}${result.change.toFixed(2)}吨，` +
           `${result.changeRateType}${result.changeRate.toFixed(2)}%`;
}
```

### 4. 非油销售环比与同比

- **环比**：使用 `todayNonOil - yesterdayNonOil` 与 `changeRate = yesterdayNonOil > 0 ? change / yesterdayNonOil × 100 : 0`。
- **同比**：当 `nonOilSales.lastYear > 0` 时，才计算 `lastYearChange` 与 `lastYearChangeRate` 并插入报告；否则该段完全省略。
- **月累计**：依赖 `calculateMonthlyNonOilTotal()` 的结果，格式为 `本月累计{X}元`。

```javascript
const yesterday = state.yesterdayData.nonOilSales || 0;
const change = today - yesterday;
const changeRate = yesterday > 0 ? (change / yesterday) * 100 : 0;

const lastYear = state.formData.nonOilSales.lastYear || 0;
const yearComparisonText = lastYear > 0
    ? `，同比去年${getChangeText(lastYearChange)}${Math.abs(lastYearChange).toFixed(1)}元，` +
      `${getRateText(lastYearChangeRate)}${Math.abs(lastYearChangeRate).toFixed(2)}％`
    : '';
```

### 5. 测评率计算（优化算法）

#### 基础公式
- **公式**: `测评率 = (实际评价数量 / 三端订单消费) × 100%`
- **精度**: 保留2位小数
- **范围**: 0% - 100%

```javascript
function calculateRatingRate(actualReviews, totalOrders) {
    if (totalOrders === 0) {
        return 0; // 避免除零错误
    }
    const rate = (actualReviews / totalOrders) * 100;
    return Math.min(100, Math.max(0, Math.round(rate * 100) / 100)); // 限制在0-100%范围内
}
```

#### 测评率等级评估
```javascript
function getRatingLevel(rate) {
    if (rate >= 90) return { level: '优秀', color: 'success' };
    if (rate >= 80) return { level: '良好', color: 'primary' };
    if (rate >= 70) return { level: '一般', color: 'warning' };
    return { level: '需改进', color: 'danger' };
}
```

### 5. 累计核销计算

#### 电子券累计核销
- **公式**: `今日累计核销 = 昨日累计核销 + 当日核销`

```javascript
function calculateCumulativeVouchers(yesterdayTotal, todayCount) {
    return yesterdayTotal + todayCount;
}
```

## 📊 数据验证规则

### 1. 数值范围验证
```javascript
const validationRules = {
    gasoline: { min: 0, max: 50, unit: '吨' },
    diesel: { min: 0, max: 20, unit: '吨' },
    nonOilSales: { min: 0, max: 50000, unit: '元' },
    orders: { min: 0, max: 1000, unit: '笔' },
    reviews: { min: 0, max: 1000, unit: '笔' },
    vouchers: { min: 0, max: 500, unit: '张' }
};
```

### 2. 逻辑一致性验证
```javascript
function validateDataConsistency(data) {
    const errors = [];
    
    // 汽油合计验证
    const calculatedGasolineTotal = data.gasoline92 + data.gasoline95 + data.gasoline98;
    if (Math.abs(calculatedGasolineTotal - data.gasolineTotal) > 0.01) {
        errors.push('汽油合计数据不一致');
    }
    
    // 测评率验证
    if (data.actualReviews > data.totalOrders) {
        errors.push('实际评价数量不能超过订单总数');
    }
    
    // 累计核销验证
    const expectedCumulative = data.yesterdayVouchers + data.todayVouchers;
    if (data.cumulativeVouchers !== expectedCumulative) {
        errors.push('累计核销数据不一致');
    }
    
    return errors;
}
```

## 🔍 智能解析规范

### 解析目标数据
1. **油品销售数据**
   - 92#汽油销售X吨
   - 95#汽油X吨  
   - 98#汽油销售X吨
   - 共计汽油销售X吨
   - 柴油X吨
   - 柴汽合计销售X吨

2. **累计数据**
   - 本月累计销售汽油X吨
   - 柴油X吨
   - 共销售油品X吨

3. **非油销售数据**
   - 当日非油销售X元
   - 本月累计X元

### 解析正则表达式
```javascript
const parsePatterns = {
    gasoline92: /92#汽油销售?(\d+\.?\d*)吨/,
    gasoline95: /95#汽油(\d+\.?\d*)吨/,
    gasoline98: /98#汽油销售?(\d+\.?\d*)吨/,
    diesel: /柴油(\d+\.?\d*)吨/,
    gasolineTotal: /共计汽油销售(\d+\.?\d*)吨/,
    totalSales: /柴汽合计销售(\d+\.?\d*)吨/,
    monthlyGasoline: /本月累计销售汽油(\d+\.?\d*)吨/,
    monthlyDiesel: /柴油(\d+\.?\d*)吨，共销售油品/,
    monthlyTotal: /共销售油品(\d+\.?\d*)吨/,
    nonOilSales: /当日非油销售(\d+\.?\d*)元/,
    monthlyNonOil: /本月累计(\d+\.?\d*)元/,
    electronicVouchersToday: /当日电子券核销(\d+)张/,
    electronicVouchersTotal: /累计核销(\d+)张/,
    icbcPointsExchange: /工行积分兑换(\d+)笔/,
    icbcExchangeAmount: /兑换?金额(\d+\.?\d*)元/,
    icbcETC: /当日工行ETC(\d+)单/,
    threeEndOrders: /三端订单消费(\d+)笔/,
    actualReviews: /实际评价数量(\d+)笔/,
    ratingRate: /测评率(\d+)％/,
    electronicCards: /今日办理电子卡(\d+)张/
};

const matches = Object.entries(parsePatterns)
    .reduce((acc, [key, pattern]) => {
        const hit = text.match(pattern);
        if (hit) acc[key] = parseFloat(hit[1]);
        return acc;
    }, {});

if (Object.keys(matches).length >= 5) {
    // 视为解析成功，缺失字段回填 0，再写入 yesterdayData
}
```

## 📈 异常检测算法

### 1. 销量异常检测
```javascript
function checkSalesAnomaly(today, yesterday) {
    if (!yesterday.parsed) return null;
    const avg = yesterday.totalSales;
    const threshold = avg * 0.3; // 30%
    if (Math.abs(today.totalSales - avg) > threshold) {
        return {
            type: 'sales_anomaly',
            severity: 'high',
            message: `销量异常：当日${today.totalSales.toFixed(2)}吨，平均${avg.toFixed(2)}吨`
        };
    }
}
```

### 2. 环比异常检测
```javascript
function checkComparisonAnomaly(changeRate) {
    if (Math.abs(changeRate) > 50) {
        return {
            type: 'change_anomaly',
            severity: Math.abs(changeRate) > 100 ? 'high' : 'medium',
            message: `环比变化异常：${changeRate.toFixed(2)}%`
        };
    }
}
```

### 3. 测评率异常检测
```javascript
function checkEvaluationAnomaly(rate, orders) {
    if (rate < 50 && orders > 0) {
        return {
            type: 'rating_anomaly',
            severity: 'medium',
            message: `测评率过低：${rate.toFixed(2)}%`
        };
    }
    if (rate > 100 || (orders > 0 && rate === 0)) {
        return {
            type: 'rating_logic_error',
            severity: 'high',
            message: '测评率逻辑错误：请检查订单与评价数量'
        };
    }
}
```

### 4. 月累计逻辑检测
```javascript
function checkMonthlyLogic(todayMonthly, yesterdayMonthly, todayDaily) {
    if (!yesterdayMonthly) return null;
    const expected = yesterdayMonthly + todayDaily;
    if (Math.abs(todayMonthly - expected) > 0.1) {
        return {
            type: 'monthly',
            severity: 'high',
            message: '月累计数据逻辑错误：计算结果与预期不符'
        };
    }
}
```

### 5. 数据范围检测
```javascript
function checkDataRange(calculated) {
    const issues = [];
    if (calculated.totalSales < 0) {
        issues.push({ type: 'range_error', field: 'totalSales' });
    }
    if (calculated.totalSales > 1000) {
        issues.push({ type: 'range_warning', field: 'totalSales' });
    }
    if (calculated.nonOilToday < 0) {
        issues.push({ type: 'range_error', field: 'nonOilSales' });
    }
    if (calculated.evaluationRate < 0 || calculated.evaluationRate > 100) {
        issues.push({ type: 'range_error', field: 'evaluationRate' });
    }
    return issues;
}
```

## 🎨 报告格式化

### 数字格式化规范
```javascript
const formatters = {
    tons: (value) => value.toFixed(2),
    yuan: (value) => value.toFixed(2),
    percentage: (value) => value.toFixed(2),
    count: (value) => Math.round(value)
};
```

### 文字描述自动选择
```javascript
function getChangeText(value) {
    return value >= 0 ? '增加' : '减少';
}

function getRateText(value) {
    return value >= 0 ? '增幅' : '减幅';
}
```

## ⚠️ 实现现状提醒

- **重复定义**：`performFinalCalculations()` 在 `index.html` 中出现两次，后续重构需合并，避免维护风险。
- **阈值写死**：30% 销量阈值、50% 环比阈值、月累计 0.1 容差等均硬编码在方法内部，建议后续抽离为常量。
- **解析格式有限**：正则依赖固定措辞（如“当日工行ETC”），对格式变体不够友好，应计划增强容错。
- **0 视为缺失**：数据完整性检查中，多个数值字段将 `0` 判为无效，需在重构时确认是否满足业务预期。

## 🔧 实现建议

### 1. 计算精度处理
- 使用 `Math.round()` 配合倍数处理精度问题
- 避免浮点数计算误差
- 统一精度标准

### 2. 错误处理
- 除零保护
- 数据范围验证
- 异常值检测

### 3. 性能优化
- 缓存计算结果
- 批量计算处理
- 异步计算支持

## 📝 使用示例

```javascript
// 示例数据
const todayData = {
    gasoline92Liters: 10333, // 升
    gasoline95Liters: 10560, // 升
    gasoline98Liters: 200,   // 升
    dieselLiters: 353,       // 升
    nonOilSales: 3175.41,    // 元
    threeEndOrders: 126,     // 笔
    actualReviews: 101       // 笔
};

// 计算结果
const result = {
    gasoline92: calculateGasolineTons(todayData.gasoline92Liters), // 7.75吨
    gasoline95: calculateGasolineTons(todayData.gasoline95Liters), // 7.92吨
    gasoline98: calculateGasolineTons(todayData.gasoline98Liters), // 0.15吨
    diesel: calculateDieselTons(todayData.dieselLiters),           // 0.30吨
    ratingRate: calculateRatingRate(todayData.actualReviews, todayData.threeEndOrders) // 80.16%
};
```

---

**版本**: 1.1  
**更新日期**: 2025年11月7日  
**适用范围**: Gas Station 销售数据报告系统
